{
  "name": "marina-reddy-world",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "firebase-tools": "^13.0.2"
  }
}
